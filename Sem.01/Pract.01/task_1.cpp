#include <iostream>
using namespace std;

int main1() {
    int a, b;
    cout << "Dividend: ";
    cin >> a;
    cout << "Divisor: ";
    cin >> b;
    cout << "The quotient of the devision: " << (a / b) << endl;
    cout << "The remainder of the devision: " << (a % b) << endl;

    return 0;
}






